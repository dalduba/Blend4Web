Текущие проблемы/задачи:

1) Синтаксис
	а) Target node: сделать возможность иерархического выбора.
	б) Get|Set????? Call? GetAttr|SetAttr? CallMethod?? Array element
	   - Как должно выглядеть?
	в) JS Script: распарсить файл, аналогично B4W API
	г) Callback Interface: добавить OneShoot etc --параметр
	д) Callbacks->Function: убрать
	е) Declarations->Function: доделать 
	   (возможность именования сокетов и функции). Только глобальные?
	   Нужны замыкания? 
	ж) Declarations->Global Variable: доделать (задание имени и типа)
Done	з) Разобраться с операторами(Operators->):
	   Привести к общему подходу реализации через динамические сокеты.
	   Разделить логический оператор сенсоров и булевых.
	и) Выработать (еще раз проверить) подход конвертирования документация->типы.
	   Реализовать адекватный парсинг типов без лишних символов. 
	   Если в типе используется "|", то тип будет воспринят на стороне
	   редактора как "Нетипизированный" тип - _Data
	   Подход заключается в контроле типов если это возможно. 
	   При этом учитываются алиасы.
	к) Особый тип expression в for, ifelse. Как должно выглядеть?
2) Контроль синтаксиса на лету по аналогии с новыми нодами Blend4Web (Вывод ошибок).
3) Генерация кода. Редактор передает генератору дерево в формате схожем с форматом шейдерных Нод. Генератор строит синтаксическое дерево и генерирует код. В отличие от NLAScript код - это непосредственно javascript.
